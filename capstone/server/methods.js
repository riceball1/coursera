Meteor.methods({

	'findItem': function(text) {
		console.log(text);
	},

	// 'itemsDB': function() {
	// 	return items.find();
	// },

	// User is logged in to comment
	// if(! this.userId) {
	// 	throw new Meteor.Error('not-authorized');
	// }


})